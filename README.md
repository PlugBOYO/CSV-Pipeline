# CSV-Pipeline
A Python script that reads a CSV file of data records, applies multiple processing strategies (e.g., doubling numbers, checking negative values), and outputs results to a new file.  Concepts learned: OOP, generators, list comprehensions, exception handling, strategy pattern.
